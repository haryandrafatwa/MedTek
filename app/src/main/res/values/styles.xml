<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="ToolbarThemePrimary" parent="@style/ThemeOverlay.AppCompat.ActionBar">
        <!-- Customize color of navigation drawer icon and back arrow -->
        <item name="android:homeAsUpIndicator">@drawable/ic_chevron_left</item>
        <item name="colorControlNormal">@android:color/white</item>
    </style>
    
    <style name="BottomSheetDialogTheme" parent="Theme.Design.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/BottomSheetStyle</item>
    </style>

    <style name="BottomSheetStyle" parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@android:color/transparent</item>
    </style>

    <style name="CustomAlertDialog" parent="ThemeOverlay.AppCompat.Dialog.Alert">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:buttonBarPositiveButtonStyle">@style/PositiveButtonStyle</item>
    </style>

    <style name="PositiveButtonStyle" parent="Widget.AppCompat.Button.ButtonBar.AlertDialog">
        <item name="android:textColor">@color/colorPrimary</item>
    </style>

    <style name="buttonStyle" parent="@android:style/Widget.Button">
        <item name="fontFamily">@font/roboto_medium</item>
        <item name="textAllCaps">false</item>
        <item name="android:textColor">@color/text_color_selector</item>
        <item name="android:textSize">18sp</item>
    </style>

    // Ghazi

    <style name="AppTheme.TextView">
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">wrap_content</item>
    </style>

    <style name="MaterialSearchViewStyle">
        <!-- Change clear text icon -->
        <item name="searchCloseIcon">@drawable/ic_action_navigation_close_inverted</item>

        <!-- Change up icon -->
        <item name="searchBackIcon">@drawable/ic_back_frame</item>


        <!-- Background for the search bar -->
        <item name="searchBackground">@color/colorPrimary</item>

        <!-- Change text color for edit text. This will also be the color of the cursor -->
        <item name="android:textColor">@color/whiteSolid</item>

        <!-- Change hint text color for edit text -->
        <item name="android:textColorHint">#FAA1A6</item>

        <!-- Hint for edit text -->
        <item name="android:hint">@string/search_hints</item>
    </style>

    <style name="PaddedLayout">
        <item name="android:paddingRight">@dimen/default_screen_pad</item>
        <item name="android:paddingLeft">@dimen/default_screen_pad</item>
        <item name="android:paddingBottom">@dimen/default_screen_pad</item>
        <item name="android:paddingTop">0dp</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:background">@color/colorPrimary</item>
        <item name="android:orientation">vertical</item>
    </style>

    <style name="ButtonSolid">
        <item name="android:textSize">14sp</item>
        <item name="android:textColor">@color/colorPrimaryDark</item>
        <item name="android:background">@drawable/bg_btn_solid_red</item>
        <item name="android:foreground">?selectableItemBackground</item>
        <item name="android:textAllCaps">false</item>
    </style>

    <style name="ButtonSolidWhite">
        <item name="android:textSize">14sp</item>
        <item name="android:textColor">@color/colorAccent</item>
        <item name="android:background">@drawable/bg_btn_solid_gray</item>
        <item name="android:foreground">?selectableItemBackground</item>
        <item name="android:textAllCaps">false</item>
    </style>

</resources>
